
module vdb.models {

    // Song vote values.
    // Corresponds to the enum SongVoteRating.
    export enum SongVoteRating {
        Nothing     = 0,
        Like        = 3,
        Favorite    = 5
    }

}

