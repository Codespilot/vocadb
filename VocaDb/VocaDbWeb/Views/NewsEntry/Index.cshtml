@using VocaDb.Web.Helpers;
@model VocaDb.Model.DataContracts.Activityfeed.NewsEntryContract[]

@{
    ViewBag.Title = "News entries";
}

@foreach (var entry in Model) {
	@ActivityfeedHelpers.NewsEntry(entry);
}
