@using VocaDb.Web.Helpers;
@model VocaDb.Model.DataContracts.Tags.TagUsageContract[]

@Html.LinkList(Model, t => Html.ActionLink(t.TagName + " (" + t.Count + ")", "Details", "Tag", new { id = t.TagName }, null))
